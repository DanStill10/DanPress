# Workflow Rules

## Branching

- Always branch from `main`.
- Do not stack or chain branches (e.g. branch A → branch B → branch C).
- Each feature branch MUST be created directly off `main` and merged back to `main` independently.
- Before merging, pull the latest `main` and rebase if needed.
